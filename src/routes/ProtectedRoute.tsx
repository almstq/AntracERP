import { ROLES, HOLDING_ROLES, WLI_ROLES, MPL_ROLES, EMS_ROLES } from '../lib/permissions/roles';

// Re-export for backward compatibility
export { ROLES, HOLDING_ROLES, WLI_ROLES, MPL_ROLES, EMS_ROLES };
