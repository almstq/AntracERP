/**
 * Antrac ERP — Entry Point
 *
 * This file is intentionally minimal. The real app boots from main.tsx
 * which sets up the full provider stack (QueryClient, Auth, Org, Router).
 *
 * This file exists only as a reference. Delete if not needed.
 */

// App boots via main.tsx → router.tsx
// See src/main.tsx for the actual application entry point.
export {};
